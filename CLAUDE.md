# CLAUDE.md

本仓库的工作指南。作业的最终依据是 `HW4_DrawingLM.pdf`，做范围决策前请先阅读它。

## 项目是什么

**HW4 — 自回归图像块生成（Autoregressive Patch Generation）：「模型能学会一次画一个图像块吗？」**

构建一个**因果图像块 Transformer**（图像版的 GPT）：通过顺序预测图像块 token 来生成图像——
从左到右、从上到下——使用带掩码的自注意力，使每个图像块只能关注**已生成**的图像块。
随后在同一数据集上训练一个 **VAE**，从样本质量、训练速度、生成速度三个维度
将两类模型做正面对比。

这是一个**开放式、以「解读」为权重核心**的作业。AI 工具的使用被明确允许（且必须记录）。
评分为 **30% 实现 / 70% 解读、对比与表达**——写作与推理类交付物的权重高于代码本身。
不要为了代码的「巧妙」而牺牲清晰、有据可依的分析。

截止时间：2025/6/5 早 8:00。

## 核心技术规格（来自 PDF，不可更改）

- **数据集：** CIFAR-10，上采样到 **64×64**。
- **图像块：** 图像块大小 **8×8** → 每张图 **64 个图像块**（token），**外加 1 个 BOS** token。
- **因果掩码：** 显式实现。必须包含一个**证明没有「未来 token」泄漏**的测试
  （例如消融掉掩码、确认性能下降）。这是第一个技术难点，也是评分项。
- **生成：** 自回归——从 `[BOS]` 开始，预测并追加图像块，直到补全整张图（64 个 token）。
- **位置编码：** 正弦位置编码（生成顺序很重要）。
- **预测头：** 在图像块词表上做 softmax（图像块级 PixelCNN 类比）——这意味着图像块需被
  量化/tokenize 成一个词表。
- **VAE 基线：** 同一数据集、**相同的训练 epoch 数**、**相同的 batch size**，以保证对比公平。

## 五阶段流水线（必须端到端跑通）

1. **图像 tokenize** — 图像块提取 + 位置编码
2. **因果掩码** — 不可看未来 token
3. **Transformer** — 预测下一个图像块 token
4. **自回归采样** — 生成完整图像
5. **VAE 对比** — 正面分析

## 里程碑与交付物

唯一交付载体是一个**可运行的 `.ipynb`**：按里程碑用 markdown 标题分节，能**自上而下干净
运行**，每张图都有标注（坐标轴标题、图例、一句话说明）。以 `.zip` 提交，标明姓名 + 学号。

1. **因果 Transformer**（15 分）— 训练用图像块 0..t 预测图像块 t+1。
   交付：训练损失曲线 + 在留出的部分序列上做「下一图像块预测」的合理性检查面板。
   必须验证无未来 token 泄漏。
2. **自回归采样 + 注意力时间线**（25 分）— 20 个样本的生成网格 +
   **2 张带注释的注意力时间线图**（对 5 张生成图，捕捉每个生成步的注意力权重）。
   说明文字按**具体性**评分——要引用具体图像与时间步，不能是泛泛之谈。需回答：
   画图像块 t 时模型关注哪些先前图像块？是局部还是全局模式？早期图像块是否对后期有过大影响？
3. **VAE 对比**（25 分）— 三个维度：**样本质量**（视觉网格 + 测试集 FID 或重构 MSE）、
   **训练速度**（相同 batch size 下每 epoch 墙钟时间）、**生成速度**（推理时每图秒数）。
   外加一份 **300 字对比备忘**，给出使用场景推荐：(a) 快速原型、(b) 最佳样本质量、
   (c) 需要计算 p(x) 做异常检测的系统——每条都要用自己的结果论证。
4. **开放探究**（30 分）— **只选一个**问题（生成顺序 / 采样温度 / 深 vs 宽 / 类条件生成）。
   在**跑任何实验之前先写下可证伪的假设**。≥4 个数据点、≥1 张结果图、400–600 字讨论。
   你大概率会遇到与假设相矛盾的结果——**要去剖析它，不要反复重跑直到它「听话」**。
   对意外结果的推理质量是本里程碑的主要评分点。
5. **AI 使用日志**（5 分）— 每次重要的 AI 使用都要记录：提示词、产出、你改了什么、为什么。
   **必须包含 ≥1 次 AI 出错**或需要纠正的案例。

## 环境

- Python 3.12，**PyTorch 2.10 + CUDA 12.8**，多张 **RTX 4090（每张 24 GB）**。
- 用 `CUDA_VISIBLE_DEVICES` 指定 GPU（机器是共享的——开长任务前先看 `nvidia-smi`）。
- 在 4090 上，64×64 的 CIFAR-10 配这个小模型训练很快；为保证对比有效，两个模型务必
  保持相同的 epoch 数与 batch size。

## 工作约定

- 把 `.ipynb` 当作主交付物。保持 cell 幂等、按里程碑排序，使 notebook 能自上而下干净运行。
- 因果掩码泄漏测试、以及配置匹配的 VAE 对比，是显式的公平性/正确性检查——重构时不要
  悄悄破坏它们。
- 任何非平凡的 AI 辅助步骤，随手记录 AI 使用日志所需信息（不要最后再回忆补写）。
- 解读重于实现。结果反常时要如实记录，而不是抹平它——这正是评分标准奖励的。
